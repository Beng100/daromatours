# רשימת בדיקות לפני העלאה לפרודקשן — daromatours.com

נכתב: 2026-07-25.

## תוכן ואימות עובדתי (חובה לפני פרסום)

- [ ] לעבור על **כל** הפריטים ב-`content-review.md` ולאשר/לעדכן מול דרומה
- [ ] לאשר גילאי/מצב אישי/פעילות של 14 אנשי הצוות, או להסיר מידע רגיש
- [ ] לאמת "אישור משהב"ט" ו"גפ"ן" עם מסמכים בתוקף, או להסיר את הטענה
- [ ] לאמת מחירי תצפית כוכבים (1,400 / 3,000 ש"ח)
- [ ] לאמת תאריכי וזמינות פסטיבל קיץ של כוכבים 2026
- [ ] לאסוף ולהזין ביקורות לקוחות אמיתיות (הרשימה ריקה כרגע בכוונה)
- [ ] לאשר/להשלים רשימת לקוחות ולוגואים להצגה פומבית
- [ ] לייצא גוף מלא של מאמרי הבלוג (17 פוסטים + 6 עמודי "מאמר" ותיקים) מ-Wix
- [ ] להחליט לגבי עמוד "המלצות טיולים" (`/recommendations` הישן) — לבנות עמוד ייעודי או להשאיר מופנה ל-`/articles`

## תוכן — נכסים

- [ ] להחליט על תמונות חלופיות במקום שתי תמונות ה-Stock שהוסרו (`/attractions`, גלריה)
- [ ] לשקול צילומי פורטרט אמיתיים לאנשי הצוות (כרגע אווטאר ראשוני בלבד)
- [ ] לבדוק אם יש וידאו/רקעים דינמיים באתר הישן שלא נסרקו (הסריקה כיסתה תגי `<img>` בלבד)

## דומיין ואירוח

- [ ] DNS: הפניית A record של `daromatours.com` ל-IP השרת (Coolify VPS)
- [ ] Coolify: יצירת Resource → Application חדש, git source מהריפו של הפרויקט
- [ ] Coolify: הגדרת custom start command עם ה-Caddyfile שבשורש הריפו (`caddy run --config /app/Caddyfile --adapter caddyfile`)
- [ ] SSL: לוודא ש-Traefik/Let's Encrypt מנפיק תעודה אוטומטית לדומיין + www
- [ ] לבדוק בפועל (אחרי דיפלוי) שההפניות ב-Caddyfile עובדות — בייחוד 3 הכתובות בעברית (`אסטרומנטיקה`, `מסע-בר-מצווה`, `תצפיות-למוסדות-לימוד-וקייטנות`) שכן קידוד Hebrew ב-URL יכול להתנהג אחרת מול Traefik/Caddy בפועל מול בדיקה מקומית

## משתני סביבה

- [ ] `VITE_CONTACT_FORM_ENDPOINT` — כרגע לא מוגדר; ללא הגדרה, הטופס נופל בחזרה לפתיחת מייל (`mailto:`) עם כל השדות ממולאים בגוף ההודעה. **מומלץ לחבר שירות אמיתי** (Formspree, פונקציית שרת, או API דוא"ל) לפני פרסום רשמי, כדי לא להסתמך על כך שלמבקר יש תוכנת מייל מוגדרת במחשב
- [ ] Analytics: כפי שהוחלט מראש (`SETUP_GUIDE.md`) — **לא** להגדיר GA4/GTM/Google Ads כרגע; שלב נפרד עתידי
- [ ] Search Console: ליצור property חדש עבור `daromatours.com` כשמגיעים לשלב זה, ולהגיש את `sitemap.xml`

## SEO

- [x] `sitemap.xml` נוצר אוטומטית ב-build (postbuild script)
- [x] `robots.txt` מצביע ל-sitemap
- [x] Canonical, Open Graph, ותגי `<title>`/`<meta description>` ייחודיים לכל עמוד — מוטבעים גם בזמן build (prerender-heads.mjs) לטובת בוטים שלא מריצים JS
- [x] Schema.org: Organization, LocalBusiness, Service (לכל אטרקציה), BreadcrumbList — מוטמעים
- [ ] Schema מסוג Event — לא נוסף, כי לא היה מידע מספיק מאומת על תאריכים/מחירים בטוחים לאירועים ספציפיים (רק פסטיבל אחד רחב); לשקול הוספה לאחר אימות
- [ ] להגיש את ה-redirect map (`redirect-map.csv`) בפועל ב-Caddyfile ולוודא עם `curl -I` שכל ההפניות מחזירות 301 נכון אחרי הפריסה
- [ ] לשקול חיבור IndexNow / הגשה ידנית ל-Search Console לאחר העלייה לאוויר

## נגישות

- [x] Skip link, focus-visible גלוי, ARIA לתפריטים/דיאלוגים/גלריה, ניווט מקלדת מלא בתפריט ובלייטבוקס
- [x] `prefers-reduced-motion` מכובד
- [ ] מומלץ להריץ בדיקת נגישות אוטומטית (axe / Lighthouse) על build אמיתי לפני עלייה לאוויר — לא זמין כלי Playwright/דפדפן בסביבת הפיתוח הנוכחית

## ביצועים

- [x] כל התמונות הומרו ל-WebP עם lazy loading ו-width/height מפורשים
- [x] Code splitting לפי עמוד (`React.lazy`)
- [x] גופנים עבריים (Rubik, Heebo) בטעינה עצמית (`@fontsource`, לא Google Fonts CDN חיצוני)
- [ ] מומלץ להריץ Lighthouse על ה-build האמיתי (לא `vite preview` — ראו הערה ב-SETUP_GUIDE שהוא לא מדמה נכון prerendered nested index.html)

## טפסים

- [x] ולידציה בצד לקוח (שדות חובה, פורמט טלפון/מייל, אישור פרטיות חובה)
- [x] מניעת שליחה כפולה (disabled בזמן שליחה)
- [x] מצבי טעינה/הצלחה/שגיאה
- [ ] לחבר endpoint אמיתי לטופס (ראו "משתני סביבה" למעלה)

## גיבויים ותשתית

- [ ] לוודא ש-Coolify/הריפו ב-GitHub מוגדרים לגיבוי (ה-VPS כבר משרת אפליקציה נוספת, OrenExpress — לוודא בידוד בין הפרויקטים כמתואר ב-SETUP_GUIDE)
- [ ] `.credentials/` נפרד ומוגן ב-.gitignore לכל טוקן/מפתח SSH ספציפי לפרויקט הזה

## בדיקות טכניות שבוצעו בפועל (בסבב הנוכחי)

- [x] `npm run typecheck` — עובר ללא שגיאות
- [x] `npm run lint` — עובר ללא שגיאות/אזהרות
- [x] `npm run build` — מצליח, כולל שלב ה-postbuild (prerender + sitemap)
- [ ] **לא בוצעה** בדיקת דפדפן אמיתית (Playwright/Browser MCP לא היו זמינים בסביבה זו) — ראו הערה בסיכום הסופי
